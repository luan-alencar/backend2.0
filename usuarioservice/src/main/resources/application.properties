#spring.datasource.url=jdbc:postgresql://localhost:5432/usuarioservice

# Aplica??o
spring.application.name=usuarioservice
com.unifacisa.shoppingcartservice-url=http://localhost:8081

# Datasource
spring.datasource.url=jdbc:h2:mem:usuarioservice
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# H2 Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA
spring.jpa.open-in-view=true

# PORT
server.port=8080

# SCHEDULING
scheduler.enabled=false
interval=10000
interval-in-cron=0/5 * * * * *
